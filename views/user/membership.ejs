<%- include("../partials/userheader") %>

    <div class="content">
        <div class="about-heading">
            <h1><i class="fas fa-crown"></i> Membership</h1>
            <p>
                Membership have many function more than general user got! <br>
                You can get more special benefit for special guest. <br>

                <div class="benefit-member">
                    <table>
                        <tr>
                            <th>BENEFIT</th>
                            <td> <h2> <i class="fas fa-user"></i> GENERAL USER</h2></td>
                            <td> <h2> <i class="fas fa-crown"></i> MEMBERSHIP</h2></td>
                        </tr>
                        <tr>
                            <th>Choose Default Template</th>
                            <td><i class="fas fa-check" style="color: rgb(0, 255, 0);"></i></td>
                            <td><i class="fas fa-check" style="color: rgb(0, 255, 0);"></i></td>
                        </tr>
                        <tr>
                            <th>Unlimited Project</th>
                            <td><i class="fas fa-times" style="color: rgb(255, 0, 0);"></i></td>
                            <td><i class="fas fa-check" style="color: rgb(0, 255, 0);"></i></td>
                        </tr>
                        <tr>
                            <th>Upload Picture Maximum : 5</th>
                            <td><i class="fas fa-times" style="color: rgb(255, 0, 0);"></i></td>
                            <td><i class="fas fa-check" style="color: rgb(0, 255, 0);"></i></td>
                        </tr>
                    </table>
                </div>
                

                <br><br> Check it here below <i class="fas fa-chevron-down"></i>
            </p>

            <% if(locals.user.status != 'membership') { %>
                <form action="/portfolio/changetomember/<%= user._id %>" method="POST">
                    <button type="submit" id="statusbtn" name="status"><i class="fas fa-crown"></i> To be Membership</button>
                </form>
            <% } %>

            <% if(locals.user.status == 'membership') { %>
                <form action="/portfolio/changetouser/<%= user._id %>" method="POST">
                    <button type="submit" id="statusbtn" name="status"><i class="fas fa-user"></i> To be General user</button>
                </form>
            <% } %>

        </div>
    </div>

        <%- include("../partials/copyright") %>
<%- include("../partials/indexfooter") %>